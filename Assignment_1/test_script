I was unaware that the function input() in python returned a string, so there are two different ways I attemped 
to create this calculator. Both which, are case sensitive. 

The first attempt is great, but restricting the input() to being a integer created a new problem. 
If the user entered anything that was not a integer (character or negative number) the program throws a error. 
This is great for characters and symbols, but not so great for numbers less than zero. 

The second attempt is stronger, allowing for the user input() to be a float. This now though creates the problem
that if the user inserts a character or symbol right off the back, the program does not run. 

Both cases allow for user regulation by being contained in a While loop, and using If statements, 
and each operation is defined in its own function, including an error for dividing by zero. 

Additionally, some classmates and online mentioned this function Try and Except. I wrote in the actual code script
that I have never worked with such a function nor do I understand how it works currently. My python knowledge
is limited to two classes and I am excited to explore for of what this langauge is capable of. 
Thank you for your time and feedback! 
